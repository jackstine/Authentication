{
  "name": "@nodeauth/authentication",
  "version": "0.0.12",
  "description": "Authentication",
  "main": "src/index.js",
  "scripts": {
    "test": "export AUTH_ENV=dev && mocha --file src/tests/setup_test.js $(find src/tests/* -name '*.nc.test.js')",
    "test-complete": "export AUTH_ENV=dev && mocha --file src/tests/setup_test.js $(find src/tests/* -name '*.complete.test.js')",
    "test-all": "export AUTH_ENV=dev && mocha --file src/tests/setup_test.js $(find src/tests/* -name '*.test.js')"
  },
  "keywords": [
    "authentication"
  ],
  "author": "Jacob Cukjati",
  "license": "ISC",
  "dependencies": {
    "bluebird": "^3.7.2",
    "jsonwebtoken": "^8.5.1",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "chai": "^4.3.4",
    "dotenv": "^8.2.0",
    "pg": "^8.6.0"
  }
}
